{
  "name": "integradraw-web",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "validate": "node scripts/validate-site.mjs",
    "validate:release": "node scripts/validate-release.mjs",
    "check": "tsc --noEmit && vitest run && npm run validate && npm run validate:release"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
