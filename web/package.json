{
  "name": "integradraw-web",
  "private": true,
  "version": "1.1.2",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "validate": "node scripts/validate-site.mjs",
    "release": "node scripts/release-cli.mjs",
    "validate:release": "node scripts/release-cli.mjs metadata",
    "check": "tsc --noEmit && vitest run && npm run validate && npm run validate:release"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
